const SearchButton = () => {
    return (
        <button type="submit" className={`w-full text-center p-3 rounded-lg bg-[#5f121a] text-[#ffffff]`}>
            Search
        </button>
    );
};

export default SearchButton;